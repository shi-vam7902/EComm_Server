node_modules
.env
dist\
```
This is a basic example of a `package.json` file. You can add more scripts, dependencies
and other configurations as needed.
### Step 3: Install Dependencies
```bash
npm install
```
### Step 4: Create a `.env` file
Create a `.env` file in the root of the project and add the following environment variables:
```env
PORT=3000
```
### Step 5: Start the Server
```bash
npm start
```
This will start the server and make it available at `http://localhost:3000`. You can
change the port number by updating the `PORT` environment variable in the `.env` file.
### Step 6: Test the Server
You can test the server by sending a `GET` request to `http://localhost:3000/` using
a tool like `curl` or `Postman`. You should see a response with the message `Hello, World!`.
```bash \